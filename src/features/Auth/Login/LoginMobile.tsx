import { LoginTypes } from "./Login";

export default function LoginMobile({
  isPasswordVisible,
  password,
  role,
  setIsPasswordVisible,
  setPassword,
  setRole,
  setUsername,
  username,
}: LoginTypes) {
  return <div></div>;
}
