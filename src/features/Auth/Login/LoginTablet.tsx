import { LoginTypes } from "./Login";

export default function LoginTablet({
  isPasswordVisible,
  password,
  role,
  setIsPasswordVisible,
  setPassword,
  setRole,
  setUsername,
  username,
}: LoginTypes) {
  return <div></div>;
}
